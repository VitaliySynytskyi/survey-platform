apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: survey-platform
    service: auth-service
spec:
  selector:
    app: survey-platform
    service: auth-service
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP 